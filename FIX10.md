Make the default chat widget bigger on desktop in index.html. Only touch index.html. Do not touch server.js. Do not change mobile styles.

Find the CSS for .chat-widget (the chat popup container). Increase the default size:
- width: 420px (was likely 380px or smaller)
- height: 580px (was likely 500px or smaller)

Also increase the chat messages area height so more conversation is visible.

Do not change the expand button, do not change mobile CSS, do not change any other styles. Verify syntax before finishing.
