# FIX14 — Update X / Twitter Link in Footer

In `index.html`, find the Twitter/X social link in the footer. It currently has a placeholder href (likely `#` or `https://twitter.com`).

Update it to:
```
href="https://x.com/NeuralFlow_AI"
```

Also make sure it opens in a new tab:
```
target="_blank" rel="noopener noreferrer"
```

That's the only change. Do not touch anything else.
